# Changelog

## [Unreleased]

### 追加

- ホームを相手一覧からダッシュボード型に変更し、トップから取引を作れるように
  - **全体の残高**：差し引き（受け取る／返す）を大きく出し、その下に「貸している」「借りている」の合計を並べる。
    アーカイブ済みの相手は数に入れない。これまでの「貸している／借りている」の2枚のカードはこれに置き換える
  - **相手ごとの残高**はこれまでどおり（相手の追加ボタンはセクションの見出しに移動）
  - **最近の取引**：新しい順に5件。6件以上あれば「すべての取引」へのリンクを出す
  - **FAB をホームにも表示**：これまではすべての取引・相手ページだけで、トップから取引を作る導線がなかった
  - ボトムバーの1つ目のタブを「相手」から「ホーム」に改名
  - 取引フォームの相手を「最後に自分で記録した取引が新しい順」に並べる（自動発生の利息は数えない）。
    相手がまだいないときはホームで追加するよう案内を出す
  - 詳細は [docs/04-screens.md](./docs/04-screens.md) の「4.5 ホーム画面 レイアウト」
- DB を Supabase（PostgreSQL）から Cloudflare D1（SQLite）に、ORM を Prisma から Drizzle ORM に移行
  - スキーマは `src/db/schema.ts`、マイグレーションは `drizzle/`（`npm run db:generate` / `db:migrate:local` / `db:migrate:remote`）。
    デプロイのワークフローで本番の D1 にマイグレーションを当ててからデプロイする
  - DB クライアントは `src/lib/db.ts`（`env.DB` のバインディングから作る）。`next dev` でもローカルの D1 を使う
  - 年利を `Decimal(6,2)` の % から、整数のベーシスポイント `Ledger.annualInterestRateBp`（5.25% → 525）に持ち替え
  - 利子ジョブの「利息の取引の作成」と「`lastInterestAccruedAt` の更新」を `db.batch()` で1つのトランザクションに
    （Prisma の D1 アダプタは `$transaction` を無視して別々に実行するため、Drizzle に移した）。相手と最初の口座の作成も同様
  - 管理画面の検索の `mode: "insensitive"` を、`%` `_` をエスケープした `LIKE`（`src/db/sql.ts` の `contains()`）に置き換え
  - 統計の相手ごとの集計を、相手1人につき4クエリから `GROUP BY` の1クエリに
  - Supabase のデータを D1 に移す `scripts/migrate-supabase-to-d1.ts` を追加（手順は `docs/11-cloudflare-workers.md` の 11.7）
  - `keep-supabase-alive.yml`、本番 DB へのワンショット移行ワークフロー（`migrate-*.yml`）、`prisma/`（スキーマ・移行履歴・
    `seed.ts`・`import-csv.ts`）、`supabase/` を削除
- ホスティングを Vercel から Cloudflare Workers に移行（OpenNext / `@opennextjs/cloudflare`）。DB は Supabase のまま
  - `wrangler.jsonc`・`open-next.config.ts` を追加し、`npm run preview` / `npm run deploy` でビルド・デプロイできるように
  - Next.js を 16.3 系に更新（OpenNext の対応バージョン）
  - Prisma クライアントの生成先を `node_modules/.prisma/client` に戻し、`@prisma/client` から読み込むように
    （Workers では edge 版に解決させて WASM を読むため）
  - Workers ではリクエストごとに Prisma クライアントを作るように（`src/lib/prisma.ts`）。Hyperdrive のバインディングがあれば自動で使う
  - 手順は `docs/11-cloudflare-workers.md`
- 週次自動利子ジョブを GitHub Actions（`weekly-interest.yml`）から Cloudflare Workers の Cron Triggers に移行
  - `worker.ts` を Worker のエントリにして `scheduled` を追加。Worker 内部から `/api/cron/weekly-interest` を呼ぶ
    （外から叩いても使い捨てトークンがないので 404）
  - 自動実行の結果を監査ログ（`SCHEDULED_INTEREST_JOB`）に残し、管理画面の「ジョブ」ページに「最後の自動実行」を表示
- 取引カードと取引詳細ダイアログのUIを刷新：用途・タイプ・金額・残高・日時を整理し、認証ページと公開ページで見た目を統一
  - **取引の情報と残高の情報を分離**：カードは区切り線の上に「名目・用途・金額・取引日時」、下に「この取引の後の残高」を置く
  - **残高は債権か債務かが読めるように**：符号ではなく「貸している残高 / 借りている残高」ということばと色（緑＝債権 / 赤＝債務）で示す
  - **名目を4通りに**：金額の向きだけでなく、**その取引を反映する前の残高**と組み合わせて
    「貸した / 返済された / 借りた / 返済した」を出し分ける。利息は「利息」として別扱い
  - **公開ページは相手視点**：符号を反転してから名目・残高の表現を決めるので、オーナーの「貸した」は相手側で「借りた」になる
  - **カードとダイアログを両ページで共通化**：公開ページでもカードをタップすると詳細ダイアログが開くように。
    編集・アーカイブ・削除のフッターは認証ページだけに表示する
  - **カードは上段＝金額以外の取引の情報、下段＝左に金額・右に残高**：下段の左右を「ラベル＋右寄せの数字」という
    同じ組み方にして位置とサイズをそろえ、一覧では金額も残高もそれぞれ縦にそろう。
    用途は上段で横幅をまるごと使えるので、金額が大きくても省略されにくい
  - カードの残高のラベルは「残高」だけにして、債権か債務かは色で示す（ことばでの説明は詳細ダイアログで出す）
  - メモがあるときはカードの用途の下に本文を出す（2行まで。全文は詳細ダイアログで読める）
  - **金額は色を付けず黒で統一**：マイナスのときだけ符号を付ける。色は残高（債権＝緑 / 債務＝赤）に取っておく
  - カードの日時は一昨日より前なら日付だけにして、名目・口座・相手のチップが並ぶ余地を残す（時刻はダイアログで確認できる）
  - ダイアログの残高は「取引前 → 取引後」を債権・債務つきで併記。作成・更新日時はカードから外してダイアログに集約
  - 詳細は [docs/04-screens.md](./docs/04-screens.md) の「4.11 取引カード・取引詳細ダイアログ」

- 管理画面（`/admin`）を新設：運営者がツケカン全体の状況を把握し、運用上の異常に対処するための画面
  - **認証は Cloudflare Access**：アプリ本体のログイン（JWT + Cookie）とは独立。Access が署名したJWTを
    チーム用JWKSで検証する。ゲートは3層（Cloudflare Access → `src/proxy.ts` → 各ページ・各Server Actionの `requireAdmin()`）
  - **ダッシュボード**：規模（アカウント・相手・口座・取引）、お金の総量（貸借残高・未払利息・累計の貸出/返済）、
    そして「気にかけるべき異常」を1画面にまとめる
  - **アカウント**：一覧（名前・メールで検索）と詳細（相手・口座・直近の取引・共有リンクの状態）
  - **口座**：口座ごとの残高と利子の設定。**利子つきなのに8日以上利息が発生していない口座を「停止の疑い」として検出する**
    （週次ジョブが失敗しても誰も気づけない問題への対策）
  - **取引**：全アカウント横断の検索（キーワード・アカウント・種別・期間・アーカイブの有無）
  - **共有リンク**：発行済みリンクの棚卸しと失効。未認証で残高が読める唯一の経路なので、いま何本生きているかを可視化する
    （トークンの値そのものは表示しない）
  - **ジョブ**：利子ジョブの状況・試し打ち（dry-run）・手動実行。GitHub Actions が落ちているときの代替手段
  - **監査ログ**：管理画面から行った操作の記録（`AdminAuditLog`）
  - 管理画面からできる書き込みは「共有リンクの失効」「利子ジョブの手動実行」の2つだけで、どちらも必ず監査ログに残る。
    取引やアカウントの編集・削除はできない
  - 環境変数：`CF_ACCESS_TEAM_DOMAIN` / `CF_ACCESS_AUD`（必須）、`ADMIN_EMAILS`（許可リスト・任意）、
    `ADMIN_DEV_EMAIL`（ローカル用の抜け道・`NODE_ENV=production` では無効）
  - 詳細は [docs/10-admin.md](./docs/10-admin.md)

- 利子システムを大幅改修：利息を元本と分けて管理し、利率・発生タイミングを口座ごとに設定できるように
  - **利息を元本と分離**：自動発生した利息は元本残高には足さず「未払利息」として別勘定にたまる
  - **返済はまず利息から**：返済を記録すると未払利息に充当され、0になってから元本が減る
  - **利率は年利で指定**：週ごとの利息は「年利 ÷ 52週」で計算。残高5,000円を境に利率を切り替える2段階制は廃止
    （移行時は「5,000円以上」の週利率を52倍して年利に変換）
  - **発生曜日を選べる**：これまで毎週水曜固定だったのを、口座ごとに好きな曜日を選べるように
  - **単利／複利を選べる**：単利は元本のみ、複利は元本＋未払利息に利息がつく（既定は単利）
  - **口座の設定ページを新設**（`/ledgers/[id]/settings`）：「毎週◯曜日に、元本に対して年利◯%（週◯%）の利息が発生します」という
    ルールの説明と、いまの残高にあてはめた次回の発生額を表示
  - 残高カードは合計を大きく出し、その下に「元本 ｜ 未払利息」の内訳を併記
  - 利息の取引には「利息」バッジを表示。ヘルプに「口座と利子」の説明を追加
  - 既存の取引はすべて通常取引（元本）として扱われる（過去の利子取引の遡及的な分離は行わない）

- 取引ボタンの表示プリセットを設定ページに追加：金額の向きを表す言い方を3種類から選べるように
  - 「貸し借り両方」（既定・貸した・返済した / 借りた・返済された）、「よく貸す」（貸した / 返済された）、「よく借りる」（返済した / 借りた）
  - 記録される金額は変わらず、ボタンのラベルと開いたときに選ばれている側だけが変わる
  - ボタンの並び順は常に「+ が左」で固定

- 取引に「用途」を追加：これまでメモに入れていた「麻雀」「ランチ」などの用途を独立した1行の項目（100文字以内）として記録できるように
  - 既存の取引のメモは用途へ自動で移植され、メモは空の状態にリセットされる
  - 過去の用途からのサジェストは用途欄に移動
- 取引のメモを詳細テキストに拡張：改行を含む複数行・1000文字以内で入力できるように
  - 取引カードにはメモの有無をアイコンで表示し、詳細モーダル・共有リンク画面で全文を表示
  - 検索は用途・メモの両方を対象に

- 公開ページのメモ：共有リンクの公開ページ（`/share/[token]`）に表示するメモを相手ごとに1つ持てるように
  - 相手詳細画面の「公開ページのメモ」から書き換える（100文字以内）。空にすると公開ページには表示されない
  - 公開ページでは「◯◯さんからのメモ」として読み取り専用で表示

### 変更

- **メモ機能を廃止し、公開ページのメモに作り直した**：相手に対して複数のメモを記録する機能（`LedgerNote`）をやめ、
  メモは「公開ページに表示する、相手ごとに1つのメモ」（`Partner.shareNote`）だけになった
  - 移行時、相手ごとにいちばん新しいメモ1件だけが公開ページのメモへ引き継がれ、残りのメモは削除される
    （ワンショット移行ワークフロー `migrate-partner-share-note.yml`）
  - 口座ごとのメモという区分もなくなり、メモは相手（公開ページ）に紐づく
- 週次利子ジョブのロジックを `src/lib/interest-job.ts` に切り出し、GitHub Actions（`scripts/weekly-interest.ts`）と
  管理画面の手動実行で共用するように。`scripts/weekly-interest.ts` は `--dry-run` に対応し、
  DBを変更せずに結果の見積もりだけを出せるようになった
- Next.js 16 で非推奨になった `middleware.ts` を `proxy.ts` に改称（動きは同じ）

- 画面の主役を「口座」から「相手」に変更：相手ページに口座をまとめ、口座ごとのページを廃止
  - **ホーム（`/`）が相手一覧に**：相手ごとの合計残高・直近の取引・口座数を並べる。相手の追加もここから
    （旧「相手の管理」ページ `/partners` は `/` へのリダイレクトになり、メニューからは外れた）
  - **相手ページ（`/partners/[id]`）を整備**：合計残高、口座の一覧（口座ごとの残高つき）、共有リンク、メモ、
    そして**すべての口座の取引をまとめた履歴**を1ページに集約
  - **口座カードに残高を表示**：利子ありの口座は「元本／未払利息」の内訳と、次回の利子の見込み額も出す
  - **口座ごとの絞り込み**：相手ページ・公開ページとも、口座カードをタップするとその口座だけに絞り込める
    （絞り込みの状態はURLの `?ledger=` に入るので、共有やブックマークもできる）
  - **口座詳細ページ（`/ledgers/[id]`）を廃止**：内容は相手ページに統合。口座の設定ページ
    （`/ledgers/[id]/settings`）はそのまま残る
  - ボトムバーの1つめのタブを「口座」から「相手」に変更

- 公開ページ（`/share/[token]`）を口座ごとから相手ごとに変更
  - 1本のリンクでその相手のすべての口座（合計残高・口座ごとの残高・取引履歴・メモ）を共有する
  - 公開ページでも口座ごとに絞り込める
  - 共有トークンは Partner が持つようになった。**発行済みのリンクはトークンの値ごと引き継がれるので、配布済みのURLはそのまま使える**
    （1人の相手が複数の口座でリンクを発行していた場合は、有効期限がいちばん先のものだけが残る）

### 改善

- 取引の金額ボタンを名目に合わせた表現に変更：「貸した」/「借りた・返済」から「貸した・返済した」/「借りた・返済された」へ
  - どちらのボタンが「返済」に当たるのか読み取れなかった曖昧さを解消
  - ボタンの色をアプリ共通の規約（+ = 緑 / - = 赤）に合わせ、登録時と一覧で色の意味が揃うように
- 残高の表現を「見ている人の視点」に統一
  - 口座の取引一覧ページ：ユーザー視点。債権なら緑で「〇〇さんから受け取ります」、債務なら赤で「〇〇さんに返します」
  - 共有リンクのページ：相手視点。残高だけでなく取引カードの金額・残高・貸し借りバッジも相手から見た向きに反転
  - 緑 = 見ている人の債権 / 赤 = 見ている人の債務、という意味が全画面で一貫するように

## 2.1.0 - 2026-04-19

- **type**: MINOR
- **title**: IA再設計・相手詳細ページ新設・取引フィルター強化

### 追加

- 相手詳細ページを新設し、取引履歴・残高・共有リンクをまとめて確認できるようになりました
- すべての取引を横断的に確認できる「すべての取引」ページを新設しました
- 統計ページを新設しました
- 検索・絞り込みシートを追加し、条件付きフィルタリングに対応しました
- 共有リンクに「リンクを開く」ボタンを追加しました

### 改善

- ホームを相手一覧ページに変更し、残高・最終取引日を一目で確認できるようになりました
- ボトムバーを相手・すべての取引・統計・メニューの4タブ構成に変更しました
- 取引カードに相手との累積残高を表示するようになりました
- 残高カード・共有リンクカード・取引カードのデザインをエメラルドテーマに刷新しました
- ページに応じた取引作成ボタンの表示を最適化しました
- 取引金額のソートを絶対値ベースに変更しました
- 相手の名前変更・アーカイブ・削除を相手詳細ページからインラインで操作できるようになりました
- 精算機能を廃止し、UI をシンプルにしました

## 2.0.0 - 2026-04-08

- **type**: MAJOR
- **title**: UI全面刷新・URL共有・ヘルプ追加

### 追加

- 取引内容をリンクで共有できる URL 共有機能を追加しました
- ヘルプページを追加しました
- N キーで取引作成モーダルを開くキーボードショートカットを追加しました

### 改善

- 取引の作成・編集モーダルを刷新し、アーカイブ機能を追加しました
- ホーム画面をタブなし単一スクロールページに刷新しました
- 取引一覧・相手カードをカード表示に変更し、フィルタ機能を追加しました
- 取引モーダルの相手選択 UI をボタングリッドに変更しました
- テーマカラーをエメラルドグリーンに変更しました
- 統計ページ・取引ページの UI を改善しました
- ボトムバーをメニューボタンに変更し、設定・ヘルプへの導線を追加しました
- 設定ページにバージョン表示を追加しました

## 1.0.0 - 2026-03-27

- **type**: MAJOR
- **title**: 初回リリース

### 追加

- パートナーとの貸し借り残高のホーム画面一覧表示
- 取引の登録・編集・削除
- 取引履歴のコピー機能
- パートナー詳細ページでの残高確認
- 「相手から」ページでのパートナーごとの受取状況確認
- グループ機能（複数人での貸し借り管理）
- 設定画面
- エラーページ・Not Found ページ
- トースト通知
- ダークモード対応
- PC 向けレイアウト対応
- メール・パスワードによる認証（サインアップ・ログイン）
