# 4. 画面設計

## 4.1 画面一覧

| 画面名   | パス           | 説明                            |
| -------- | -------------- | ------------------------------- |
| ログイン | /login         | ユーザー名/パスワードでログイン |
| 新規登録 | /register      | アカウント作成                  |
| ホーム（相手一覧） | /   | 貸している／借りている合計・相手ごとの合計残高・直近の取引・相手の追加 |
| 相手詳細 | /partners/[id] | 合計残高と、その下に口座ごとのカード（口座が複数のときだけ口座ごとの残高を出す）・共有カード（リンクのコピー・プレビュー・再発行と無効化のメニュー・リンクがあるときだけ共有メモ）・全口座の取引履歴（口座のチップで絞り込み可）。ヘッダーに統計・設定へのアイコン |
| 相手の統計 | /partners/[id]/statistics | 相手ごとの集計（現在は骨組みのみ） |
| 相手設定 | /partners/[id]/edit | 名前の変更・アーカイブ・削除 |
| 口座設定 | /ledgers/[id]/settings | 口座名・年利・利息の発生曜日・単利/複利・口座の削除 |
| 公開ページ | /share/[token] | 相手ごとの読み取り専用ページ（記録者のメモつき・口座で絞り込み可） |
| 設定     | /settings      | プロフィール編集・取引ボタン表示・外観 |
| 取引登録 | (モーダル)     | 新規取引の入力フォーム          |

### 管理画面（運営者向け・Cloudflare Access で認証）

アプリ本体のスマホ前提の1カラム + ボトムバーとは別で、PCで一覧を見るためのサイドバー構成。
詳細は [10-admin.md](./10-admin.md)。

| 画面名 | パス | 説明 |
| --- | --- | --- |
| ダッシュボード | /admin | 全体サマリー（規模・お金の総量）と気にかけるべき異常 |
| アカウント一覧 | /admin/accounts | 名前・メールで検索。残高・相手数・口座数・取引数 |
| アカウント詳細 | /admin/accounts/[id] | そのユーザーの相手・口座・直近の取引・共有リンクの状態 |
| 口座一覧 | /admin/ledgers | 口座ごとの残高と利子の設定。利子ジョブが止まっていそうな口座を検出 |
| 取引検索 | /admin/transactions | 全アカウント横断（キーワード・アカウント・種別・期間・アーカイブ） |
| 共有リンク | /admin/share-links | 発行済みリンクの棚卸しと失効 |
| ジョブ | /admin/jobs | 利子ジョブの状況・試し打ち・手動実行 |
| 監査ログ | /admin/audit | 管理画面から行った操作の記録 |

---

## 4.2 ナビゲーション設計

### ボトムバーナビゲーション採用

| 方式                   | メリット                    | デメリット                       |
| ---------------------- | --------------------------- | -------------------------------- |
| ハンバーガーメニュー   | 画面広く使える              | タップ数が増える、発見されにくい |
| **ボトムバー（採用）** | 主要機能に1タップでアクセス | 画面下部を占有、項目数に制限     |

**採用理由:**

- 画面数が少ない（4項目）→ ボトムバーに収まる
- 頻繁に使う機能へのアクセスを最優先
- スマホの片手操作で親指が届きやすい

### ボトムバーの項目（確定）

| アイコン | ラベル       | パス            | 説明                         |
| -------- | ------------ | --------------- | ---------------------------- |
| 🏠       | ホーム       | `/`             | 全体の残高・相手ごとの残高・最近の取引（メイン機能） |
| 📋       | すべての取引 | `/transactions` | 全取引の一覧                 |
| 📊       | 統計         | `/statistics`   | 貸借の集計                   |
| ☰        | メニュー     | `/menu`         | 相手管理・設定・ヘルプ       |

**設定ページをボトムバーに入れない理由:**

- 使用頻度が低い
- ヘッダーの⚙️アイコンからアクセス可能

**取引登録はFAB（右下の+ボタン）で対応:**

- 最も頻繁に使う操作なので常に目立つ位置に配置
- モーダルで開くのでページ遷移不要
- 表示するのはホーム・すべての取引・相手ページ（`src/components/layouts/fab-controller.tsx`）。
  相手ページではその相手（口座で絞り込み中ならその口座も）を初期選択する。ほかの画面では相手は未選択で開く

---

## 4.3 画面遷移図

```
[ログイン] ─── 認証成功 ───> [ホーム]
                               │
                               ├── 相手ごとの残高
                               │     └── 相手タップ ──> [相手詳細]
                               │
                               ├── 最近の取引
                               │     ├── 取引タップ ──> [取引詳細ダイアログ]
                               │     └── すべて見る ──> [すべての取引]
                               │
                               └── FAB(+) ──> [取引登録モーダル]
```

---

## 4.4 共通レイアウト

```
┌────────────────────────────────┐
│  貸し借り管理          [⚙️]   │  ← ヘッダー（設定アイコンのみ）
├────────────────────────────────┤
│                                │
│                                │
│        メインコンテンツ        │
│                                │
│                                │
│                         [＋]   │  ← FAB（右下固定）
├────────────────────────────────┤
│  [👥]  [📋]  [📊]  [☰]        │  ← ボトムバー
│  相手  取引  統計  メニュー    │
└────────────────────────────────┘
```

---

## 4.5 ホーム画面 レイアウト

相手一覧を主役に、上に全体の残高、下に最近の取引を置く。取引の作成は FAB から。

```
┌────────────────────────────────┐
│  ホーム                        │
├────────────────────────────────┤
│ ┌────────────────────────────┐ │
│ │ 差し引きで受け取ります     │ │ ← 全体の残高（緑=受け取る / 赤=返す）
│ │ ¥25,937                    │ │
│ ├──────────────┬─────────────┤ │
│ │ ●貸している  │ ●借りている │ │ ← 相手ごとのいまの残高を向きで分けて合計
│ │ ¥60,903      │ ¥34,966     │ │
│ └──────────────┴─────────────┘ │
│  相手ごとの残高   [相手を追加] │
│ ┌────────────────────────────┐ │
│ │ えいご [2口座]   貸している > │ │
│ │ 3日前 · ランチ代   ¥35,773   │ │  ← 債権は緑・債務は赤・精算済みはグレー
│ └────────────────────────────┘ │
│ ┌────────────────────────────┐ │
│ │ ほんじょう       借りている > │ │
│ │ 2週間前 · 立替え   ¥34,966   │ │  ← 金額は絶対値。向きは色とラベルで示す
│ └────────────────────────────┘ │
│  最近の取引        すべて見る >│
│ ┌────────────────────────────┐ │
│ │ [貸した] えいご     18:30  │ │  ← 取引カード（4.11）を5件
│ │ 麻雀                        │ │
│ └────────────────────────────┘ │
│                         [＋]   │
├────────────────────────────────┤
│  [🏠]  [📋]  [📊]  [☰]        │
│ ホーム 取引  統計  メニュー    │
└────────────────────────────────┘
```

- 実装: `src/app/(main)/page.tsx`・`src/components/features/home/`（`balance-summary-card.tsx`・`recent-transactions-section.tsx`）・`src/components/features/partner/partner-list-section.tsx`
- **全体の残高**は、差し引き（受け取る／返す）を大きく出し、その下に「貸している」「借りている」の合計を並べる。
  「貸している」「借りている」は相手ごとの**いまの残高**を向きで分けて足したもので、統計の累計（これまでに貸した額）とは別物
- 全体の残高はアーカイブしていない相手だけで計算する（統計と同じ）
- 相手がいないときは全体の残高を出さず、相手の追加を促す
- **最近の取引**は新しい順に5件。取引後の残高は口座の全履歴から積み上げるので、全件を取ってから先頭5件だけ出す。
  6件以上あるときだけ「すべて見る」を出す。取引がないときはセクションごと出さない
- 最後のカードが FAB に隠れないよう、下端に余白をとる

---

## 4.6 すべての取引画面 レイアウト

```
┌────────────────────────────────┐
│  すべての取引                  │
├────────────────────────────────┤
│  全相手の取引一覧              │
│ ┌────────────────────────────┐ │
│ │ 2025/01/15                 │ │
│ │ えいご  麻雀     +¥5,000   │ │
│ ├────────────────────────────┤ │
│ │ 2025/01/14                 │ │
│ │ おうた  ランチ   -¥1,200   │ │
│ ├────────────────────────────┤ │
│ │ 2025/01/10                 │ │
│ │ えいご  返済     -¥3,000   │ │
│ └────────────────────────────┘ │
│                         [🔍]   │  ← 絞り込み
│                         [＋]   │  ← FAB
├────────────────────────────────┤
│  [🏠]  [📋]  [📊]  [☰]        │
│ ホーム 取引  統計  メニュー    │
└────────────────────────────────┘
```

---

## 4.7 取引履歴画面 レイアウト

```
┌────────────────────────────────┐
│  < 戻る     えいご             │
├────────────────────────────────┤
│                                │
│   現在の残高                   │
│   ¥ 35,773                     │
│                                │
├────────────────────────────────┤
│  取引履歴                      │
│ ┌────────────────────────────┐ │
│ │ 2025/01/15  麻雀   +¥5,000 │ │
│ │ 2025/01/10  返済   -¥3,000 │ │
│ │ 2025/01/05  飲み会 +¥8,000 │ │
│ └────────────────────────────┘ │
│                                │
│                         [＋]   │
├────────────────────────────────┤
│  [👥]  [📋]  [📊]  [☰]        │
│  相手  取引  統計  メニュー    │
└────────────────────────────────┘
```

---

## 4.8 取引登録モーダル レイアウト

```
┌─────────────────────────────────────┐
│                                     │
│  ┌─────────────────────────────┐   │
│  │      新しい取引        [×]  │   │
│  ├─────────────────────────────┤   │
│  │                             │   │
│  │  相手                       │   │
│  │  ┌─────────────────────┐   │   │
│  │  │ 選択してください  ▼ │   │   │  ← コンボボックス
│  │  └─────────────────────┘   │   │
│  │  ┌─────────────────────┐   │   │
│  │  │ 🔍 検索...          │   │   │  ← 検索・フィルタ
│  │  ├─────────────────────┤   │   │
│  │  │ ● えいご            │   │   │  ← 既存Partner
│  │  │ ○ おうた            │   │   │
│  │  │ ○ ほんじょう        │   │   │
│  │  ├─────────────────────┤   │   │
│  │  │ ＋ 新しい相手を追加  │   │   │  ← 新規作成
│  │  └─────────────────────┘   │   │
│  │                             │   │
│  │  金額                       │   │
│  │  ┌─────────────────────┐   │   │
│  │  │ ¥                   │   │   │
│  │  └─────────────────────┘   │   │
│  │  ┌─────────────┐ ┌─────────────┐│   │
│  │  │貸した・返済した││借りた・返済された││ ← トグルボタン
│  │  │   (+) 緑     │ │   (-) 赤     ││   │
│  │  └─────────────┘ └─────────────┘│   │
│  │   ↑ ラベルは設定のプリセット次第 │   │
│  │                             │   │
│  │  説明（任意）               │   │
│  │  ┌─────────────────────┐   │   │
│  │  │                     │   │   │
│  │  └─────────────────────┘   │   │
│  │  ┌───────┐┌───────┐┌─────┐ │   │
│  │  │ 麻雀 ││ドライブ││ランチ│ │   │  ← サジェストチップ
│  │  └───────┘└───────┘└─────┘ │   │
│  │                             │   │
│  │  日付                       │   │
│  │  ┌─────────────────────┐   │   │
│  │  │ 📅 2025/01/15       │   │   │  ← カレンダーピッカー
│  │  └─────────────────────┘   │   │
│  │                             │   │
│  │  ┌─────────────────────┐   │   │
│  │  │        登録         │   │   │  ← プライマリボタン
│  │  └─────────────────────┘   │   │
│  │                             │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
      ↑ 背景はオーバーレイ（半透明）
```

---

## 4.9 取引登録フォーム 入力フロー

```
1. 相手選択
   ┌─────────────────────────────┐
   │ 相手のボタンを並べて表示    │
   │ （5人以上は3列、以下は2列） │
   │         ↓                   │
   │ タップで選択                │
   │                             │
   │ 並び順: 最後に自分で記録した│
   │ 取引が新しい順。自動発生の  │
   │ 利息は数えない。取引がない  │
   │ 相手は名前順で後ろ          │
   │ 相手ページから開いたときは  │
   │ その相手を初期選択          │
   │ 相手がいないときは、ホーム  │
   │ で追加するよう案内を出す    │
   └─────────────────────────────┘

2. 金額入力
   ┌─────────────────────────────┐
   │ 数字を入力                  │
   │         ↓                   │
   │ 左（+）or 右（-）を選択     │
   │  → 自動で符号が決定        │
   │  → 左: +金額（債権が増える）│
   │  → 右: -金額（債務が増える）│
   │                             │
   │ 名目は4通りあるが符号は2択: │
   │  + : 貸した / 返済した      │
   │  - : 借りた / 返済された    │
   │ ボタンに出す言い方は設定の  │
   │ プリセットで切り替える。    │
   │ 並び順は常に + が左で固定。 │
   └─────────────────────────────┘

3. 説明入力（任意）
   ┌─────────────────────────────┐
   │ サジェストチップをタップ    │
   │  → 入力欄に自動入力        │
   │                             │
   │ または自由入力              │
   └─────────────────────────────┘

4. 日付選択
   ┌─────────────────────────────┐
   │ デフォルト: 今日            │
   │ タップでカレンダー表示      │
   │ 過去日のみ選択可能          │
   └─────────────────────────────┘

5. 登録
   ┌─────────────────────────────┐
   │ 「登録」ボタンをタップ      │
   │         ↓                   │
   │ バリデーション実行          │
   │         ↓                   │
   │ 成功: モーダルを閉じて      │
   │       一覧を更新            │
   │ 失敗: エラーメッセージ表示  │
   └─────────────────────────────┘
```

---

## 4.10 取引登録フォーム バリデーション

| 項目 | ルール              | エラーメッセージ                                                    |
| ---- | ------------------- | ------------------------------------------------------------------- |
| 相手 | 必須                | 「相手を選択してください」                                          |
| 金額 | 必須、1〜10,000,000 | 「金額を入力してください」「金額は1,000万円以下で入力してください」 |
| 説明 | 任意、最大100文字   | 「説明は100文字以内で入力してください」                             |
| 日付 | 必須、未来日不可    | 「未来の日付は選択できません」                                      |

---

## 4.11 取引カード・取引詳細ダイアログ

取引の見た目は**認証ページ（アプリ本体）と公開ページ（`/share/[token]`）で同じコンポーネント**を使う。
違うのは「見ている人」だけなので、`viewpoint`（`"owner"` / `"partner"`）で符号と文言を切り替える。

- 実装: `src/components/features/transaction/transaction-card.tsx` / `transaction-detail-modal.tsx`
- 文言・符号の変換: `src/lib/transaction-wording.ts`
- 配色: `src/components/features/transaction/transaction-tone.ts`

### 取引カード

上段に金額以外の取引の情報、下段の左に取引の金額、右に残高を置く。
下段の左右は「ラベル＋右寄せの数字」という同じ組み方なので、位置もサイズもそろう。

```
┌──────────────────────────────────────┐
│ [貸した] [口座] 相手名        18:30  │ ← 上段: 名目・文脈・取引日時
│ 飲み会の立て替え                     │ ← 上段: 用途（カード幅を使える）
│ 駅前の居酒屋                         │ ← 上段: メモ（あるときだけ・2行まで）
├──────────────────┬───────────────────┤
│ 金額      ¥3,000 │ 残高      ¥12,000 │ ← 下段: 左=取引の金額
└──────────────────┴───────────────────┘    右=反映後の残高（色で債権/債務）
```

- **金額は色を付けず黒（foreground）で出し、マイナスのときだけ符号を付ける**。
  色は残高（債権 = 緑 / 債務 = 赤）に取っておき、取引の向きは名目チップが示す
- 残高のラベルは「残高」だけにして、債権か債務かは色で示す。
  ことばでの説明（貸している／借りている・債権／債務）は詳細ダイアログで出す
- 用途が上段で横幅をまるごと使えるので、金額が大きくても用途が省略されにくい。
  一覧では金額も残高もそれぞれ縦にそろうので、推移を列として追える
- 幅が足りないときに削るのは口座名・相手名だけ。名目チップと日時は常に残す
- 日時は取引日時のみ（`formatTransactionTime`）。60分以内は「N分前」、今日は時刻、昨日は「昨日 HH:mm」、
  **それ以前は日付だけ**（時刻は詳細ダイアログで出す）。作成・更新はカードには出さない
- メモがあれば用途の下に本文を出す。カードでは2行までにして、全文はダイアログで読ませる
- 口座名チップ・相手名は、それらが混ざって並ぶ一覧でだけ出す

### 取引の名目（タイプ）

名目は金額の向きだけでは決まらない。**その取引を反映する前の残高**と組み合わせて4通りになる。

| 反映前の残高 | 金額 +（債権が増える） | 金額 −（債務が増える） |
| --- | --- | --- |
| 0以上（貸している） | 貸した | 返済された |
| マイナス（借りている） | 返済した | 借りた |

- 利息（`kind = "INTEREST"`）は元本の貸し借りと別勘定なので、名目を「利息」（琥珀色）に置き換える
- 公開ページでは符号を反転してから判定するので、オーナーの「貸した」は相手側で「借りた」になる

### 残高の表現

残高は絶対値＋ことばで、債権か債務かがすぐ分かるようにする（符号に頼らない）。

カードでは色だけ、詳細ダイアログではことばでも示す。

| 見ている人の残高 | ことば（ダイアログ） | 立場 | 色 |
| --- | --- | --- | --- |
| プラス | 貸している | 債権 | emerald |
| マイナス | 借りている | 債務 | red |
| ゼロ | 貸し借りなし | 精算済み | muted |

### 取引詳細ダイアログ

```
┌──────────────────────────────────────┐
│ 取引の詳細                        [×] │
├──────────────────────────────────────┤
│              [貸した]                 │
│              ¥3,000                   │
│          たなかさんに貸しました        │
│                                       │
│ 取引                                  │
│ ┌───────────────────────────────────┐ │
│ │ 用途      飲み会の立て替え         │ │
│ │ メモ      駅前の居酒屋             │ │
│ │ 取引日時  2026年9月21日(月) 18:30  │ │
│ │ 口座      生活費                   │ │
│ └───────────────────────────────────┘ │
│ 残高                                  │
│ ┌───────────────────────────────────┐ │
│ │ 取引前        →         取引後     │ │
│ │ 貸し借りなし          貸している   │ │
│ │ ¥0                      ¥3,000     │ │
│ │ たなかさんから受け取る残高（債権） │ │
│ └───────────────────────────────────┘ │
│ 作成 … ・ 更新 …                      │ ← 認証ページのみ
├──────────────────────────────────────┤
│ [編集] [アーカイブ] [🗑]               │ ← 認証ページのみ（フッター）
└──────────────────────────────────────┘
```

- 金額はカードと同じく黒。マイナスのときだけ符号を付ける
- フッター（編集・アーカイブ・削除）は `actions` を渡したときだけ描画する。公開ページでは渡さないので出ない
- 作成・更新日時は記録者だけの情報。公開ページのデータには `createdAt` を含めないので自然に出ない
